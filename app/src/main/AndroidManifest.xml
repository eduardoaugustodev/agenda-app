<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alura.agendaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgendaAPP">
        <activity android:name=".ui.activity.FormularioAlunoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ListaAlunosActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; Representa a ação da activity &ndash;&gt;
                <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; Representa a categoria da activity &ndash;&gt;
            </intent-filter>-->
        </activity>
    </application>

</manifest>